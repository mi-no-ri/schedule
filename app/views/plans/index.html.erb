<h1>スケジュール管理アプリ</h1>
<div id="wrap">    
    <div id="top">
        <h2>予定一覧</h2>
        <%= link_to "新規追加", :new_plan, id: "new-plan-button" %>
    </div>
    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>タイトル</th>
                <th>開始日</th>
                <th>終了日</th>
                <th>終日</th>
                <th>更新日時</th>
            </tr>
        </thead>

        <tbody>
            <% @plans.each do |plan| %>
                <tr>
                    <td><%= plan.id %></td>
                    <td><%= plan.name %></td>
                    <td><%= plan.start %></td>
                    <td><%= plan.end %></td>
                    <td>
                        <% if plan.all_day %>
                        〇
                        <% end %>
                    </td>
                    <td><%= plan.updated_at %></td>
                </tr>
            <% end %>
        </thead>
    </table>
</div>